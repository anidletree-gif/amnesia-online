# 更新日志

## v1.2.0-beta (2026-08-30)

### 安全加固
- 全部用户 HTTP 接口新增 token 鉴权：修改昵称/签名/密码/标记/标签/头像、获取用户信息等接口均需携带有效会话 token，且 token 所属邮箱必须与操作目标一致，杜绝邮箱冒充
- WebSocket 鉴权改为首条 auth 消息握手，token 不再走 URL query，避免被 nginx 访问日志明文记录
- 修复 nginx 反代场景下的回环误判：改用 X-Real-IP 判断真实客户端来源，防止外部连接被误判为本机而绕过鉴权
- sessions.json、admin_password.txt、用户上传头像全部加入 .gitignore，敏感数据不入库
- 管理员密码改为从 admin_password.txt 读取（代码中仅保留占位符），避免真实凭据提交到仓库
- 会话过期自动清理：每 6 小时清理一次过期 token，防止 sessions.json 无限膨胀

### 游戏逻辑修复
- 修复虚构者"查验反转"标记顺序错误：先消费目标已有反转标记、再施加新标记，避免虚构者立即反转自己的查验结果
- 修复房主离开等待房时解散通知发不出的问题：先广播 room_dissolved 再删除房间
- 统一中立阵营胜利判定返回值，前端展示一致
- 观战者进房立即下发完整 state_sync（阶段/玩家/死者/聊天记录），不再空白
- 观战者改为全程禁言，避免干扰对局

### 功能新增
- 新增独立教学页 guide.html：游戏规则、常见问题 FAQ、全部角色预览，点击角色查看详情
- 对局主页右上角新增「教学」入口
- 「我的」页面新增独立的自定义标记管理（与个人标签完全独立，对局内贴玩家标签用）

### 体验优化
- 移动端软键盘弹出时自动隐藏底部悬浮导航（对局/我的），键盘收起后恢复，避免挡视角
- 修复「我的」视图被错误嵌套进 gamePanel 导致的空白与卡片溢出问题
- 修复编辑资料保存按钮无效（函数作用域）及昵称/签名拆分编辑

### 部署
- 机器人陪玩改为 systemd 服务托管，Restart=always，不再因 SSH 断开或 15 分钟超时导致房间消失

## v1.0.1-beta (2026-08-15)

- 平衡性调整：侦探获得带刀能力，暗杀仅对凶手阵营有效，不误杀好人/中立
- 夜晚/发言/投票超时放宽
- 前端公示容器：投票结果、白天死者、游戏结束统一弹层展示
